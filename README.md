#Description
A simple C++ reverse proxy server.

## Building
    export WORKSPACE=<path-to-downloaded-directory>
    cd ${WORKSPACE}
    mkdir build
    cd build
    cmake ..
    make 

## Running tests
    ${WORKSPACE}/tests/test.sh
